# Zen v0.1.0-beta.9

This beta contains the reviewed changes on `main` since `v0.1.0-beta.8`.

## What changed

- Make Calendar actions run unattended (`9b7b008`)
- Add trusted device revocation (`77edea1`)
- Make Codex follow-up submission exact-once. (`1d51d7d`)
- Merge trusted device revocation and exact-once follow-ups (`c161ceb`)
- Replace Brain Goal scheduler with Work events (`6473ecf`)
- Recover Brain event delivery and owner races (`4bbd50f`)
- Use durable Session receipts for Brain events (`a46544d`)
- Merge trusted device revocation and exact-once follow-ups after Brain integration (`be76a18`)
- Fix Brain receipt and stale reconciliation (`0b7618f`)
- Add optional pinned Zen Link transport (`30f4eef`)
- Harden Session file previews and uploads (`b3ba7e4`)
- Document optional Zen Link operations (`c5c5fd9`)
- Submit original Codex chat text atomically (`86cfc9b`)
- Keep contended chat input durably pending (`98bc0b7`)
- Stop durable churn during chat contention (`2a15c48`)
- Simplify Chat and Work event transport (`b164d39`)
- Unify interactive Session input (`6b8ec56`)
- Persist Session input receipts (`4329467`)
- Keep receipt rollback retryable (`ad7c544`)
- Filter stale Work events at terminal boundary (`b073bfe`)
- Fix Codex delegated prompt submission (`8690dca`)
- Show Brain work results in conversation (`47cee89`)
- Keep completed Brain work terminal on session cleanup (`5efff07`)
- Fix Brain event cards during cold start (`467e9d8`)
- Stabilize conversation history during live updates (`f2fd9dc`)
- Preserve preformatted timeline content (`cb880fb`)
- Make delegated Session lifecycle authoritative (`21f2e4c`)
- Keep delegated turn terminals immutable (`9962f8d`)
- Deliver Brain work events directly (`fceaee9`)
- Add code block copy action (`782e39b`)
- Refine code block copy interaction (`aa8b3c5`)
- Fix delegated input admission (`2e0e633`)
- Upgrade Brain decision alignment (`9f7a9db`)
- Automate beta release preparation (`22061d4`)
- Create release workflow build temp (`e3adea8`)
- Fix release workflow temp context (`c53865b`)

## Install

Download `zen-linux-amd64.tar.gz`, `zen-linux-arm64.tar.gz`, or `zen-darwin-arm64.tar.gz` for your host, plus the Android APK and `SHA256SUMS`. Extract the archive, install `zen` on your `PATH`, run `zen`, then create a pairing link with `zen pair https://your-origin`.

Apple Silicon support requires `tmux` (`brew install tmux`). The macOS binary is cross-built and should be treated as beta until it has completed broader real-device testing.

The iOS Preview is distributed through TestFlight at <https://testflight.apple.com/join/rTKCDzMt>, subject to Apple's beta approval and processing.

## iOS Preview identity

- Bundle: `com.daoleno.zen.preview`
- Marketing version: `0.1.0`
- TestFlight build: `10`
- Source tag: `v0.1.0-beta.9`

The iOS build number is tracked independently from Android because App Store Connect build history can be ahead.

## Android identity

- Package: `com.daoleno.zen`
- `versionCode`: `9`
- ABI: `arm64-v8a`
- Signing certificate SHA-256: `C2:FC:5B:09:B3:86:92:EE:70:59:71:1F:E7:ED:B8:79:4C:E3:65:FE:1C:7A:06:AB:95:4E:5D:D1:BD:CD:A4:FD`

Android may require permission to install from unknown sources or display a Play Protect warning. Obtainium can follow this repository's GitHub Releases. Zen does not currently provide a Play Store package; iOS Preview distribution uses TestFlight.
