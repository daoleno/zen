# Zen v0.1.0-beta.2

This beta fixes the first-run experience and adds an Apple Silicon daemon build.

## What changed

- Onboarding now respects screen safe areas and scrolls on smaller displays.
- Setup copy is shorter and focused on pairing.
- Removed migration support for the discontinued `~/.config/zen` state directory.
- Added `zen-darwin-arm64.tar.gz` for Apple Silicon Macs.
- Linux daemon downloads now use archives containing the binary and required notices.
- Release downloads no longer include duplicate Markdown and loose legal files.

## Install

Download `zen-linux-amd64.tar.gz`, `zen-linux-arm64.tar.gz`, or `zen-darwin-arm64.tar.gz` for your host, plus the Android APK and `SHA256SUMS`. Extract the archive, install `zen` on your `PATH`, run `zen`, then create a pairing link with `zen pair https://your-origin`.

Apple Silicon support requires `tmux` (`brew install tmux`). The macOS binary is cross-built and should be treated as beta until it has completed broader real-device testing.

## Android identity

- Package: `com.daoleno.zen`
- `versionCode`: `2`
- ABI: `arm64-v8a`
- Signing certificate SHA-256: `C2:FC:5B:09:B3:86:92:EE:70:59:71:1F:E7:ED:B8:79:4C:E3:65:FE:1C:7A:06:AB:95:4E:5D:D1:BD:CD:A4:FD`

Android may require permission to install from unknown sources or display a Play Protect warning. Obtainium can follow this repository's GitHub Releases. Zen does not currently provide an iOS app or Play Store package.
