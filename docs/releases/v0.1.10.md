# Zen v0.1.10

This release contains the reviewed changes on `main` since `v0.1.9`.

## What changed

- Capture Brain transcript without App subscription (`d23e969`)
- Guard Brain capture against thread switches and unchanged sources (`d29e6a8`)
- Merge remote-tracking branch 'origin/main' (`9ca6261`)
- Handle Android document providers and older picker bridges (`95b6af1`)
- Keep document metadata advisory and expose picker failure stages (`2772a6b`)
- Use one-shot Android attachment import and expose open failures (`2c3c944`)
- Add shared image previews and native attachment multi-select (`507d363`)
- Integrate native DSH Sessions and explicit service Quick Tunnels (`4491cf7`)
- Verify tunnel cleanup after daemon death (`ffb221b`)
- Map DSH and tunnel ownership verification (`c66ac49`)
- Batch startup recovery and preserve discovery ownership (`9d57908`)
- Bound public tunnel DNS publication verification (`d03c196`)
- Wait for SQLite commit in WAL cache regression fixture (`c1a69ec`)
- Unify Skill and Plugin image previews with workspace rendering (`d04f126`)
- Repair same-session recovery after superseded loss (`3c5174e`)
- Merge branch 'main' into feat/dsh-tunnels-images-20260922 (`5ecb56b`)
- Finish DSH interactions and bounded image/tunnel behavior (`5a951b9`)
- Render bounded SVG previews and quiet completed uploads (`8798dbc`)
- Put Shell first in uniform session grid (`06dc92a`)
- Bind Claude resume transcripts to native identity (`67a4c87`)
- Keep active Claude runtime aligned with native CLI (`f42d8b5`)
- Guard Claude process environment test on Linux (`a603955`)
- Route custom Claude connections through selected settings (`86860aa`)
- Enhance provider catalog metadata and picker (`e2b7b34`)
- Correct DSH native mode and Web access (`1e572d7`)
- Implement Zen provider gateway slice (`4dd1a2e`)
- Fix gateway status projection typing (`5ecfc0c`)
- Harden provider gateway routing and fallback (`8b7732e`)
- Honor disabled models in gateway resolution (`5659296`)
- Add models.dev provider metadata enrichment (`15f696b`)
- Redesign Providers settings and remove switch picker (`e799732`)
- Fix agent-first provider settings workflow (`b7a262f`)
- Allow manual Provider models and expose gateway status (`c3335ba`)
- Add local provider model fallbacks (`a618ab6`)
- Add web fallbacks for remote desktop previews (`57806e7`)
- Fix Claude session model handoff (`ddfdaea`)
- Fix Claude model ownership and routing (`cf251bb`)
- Rebuild Provider connection and model ownership (`e7faa25`)
- Clarify request model routing comments (`98c2704`)

## Install

Download `zen-linux-amd64.tar.gz`, `zen-linux-arm64.tar.gz`, or `zen-darwin-arm64.tar.gz` for your host, plus the Android APK and `SHA256SUMS`. Extract the archive, install `zen` on your `PATH`, run `zen`, then create a pairing link with `zen pair https://your-origin`.

Apple Silicon support requires `tmux` (`brew install tmux`). The macOS binary is cross-built; validate it on a real Apple Silicon host before operational use.

The iOS Preview is distributed through TestFlight at <https://testflight.apple.com/join/rTKCDzMt>, subject to Apple's beta approval and processing.

## iOS Preview identity

- Bundle: `com.daoleno.zen.preview`
- Marketing version: `0.1.10`
- TestFlight build: `32`
- Source tag: `v0.1.10`

The iOS build number is tracked independently from Android because App Store Connect build history can be ahead.

## Android identity

- Package: `com.daoleno.zen`
- `versionCode`: `34`
- ABI: `arm64-v8a`
- Signing certificate SHA-256: `C2:FC:5B:09:B3:86:92:EE:70:59:71:1F:E7:ED:B8:79:4C:E3:65:FE:1C:7A:06:AB:95:4E:5D:D1:BD:CD:A4:FD`

Android may require permission to install from unknown sources or display a Play Protect warning. Obtainium can follow this repository's GitHub Releases. Zen does not currently provide a Play Store package; iOS Preview distribution uses TestFlight.
