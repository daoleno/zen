# Zen v0.1.0-beta.20

This beta contains the reviewed changes on `main` since `v0.1.0-beta.19`.

## What changed

- Fix flaky credential hint projection test ordering (`c5418f7`)
- index on main: c5418f7 Fix flaky credential hint projection test ordering (`df9a0bf`)
- On main: zen-provider-seamless-global-switch-snapshot (`0292007`)
- Make Provider switching seamless (`f48eef4`)
- Allow opaque Codex model passthrough (`3c06469`)
- Restore model effect selection flow (`dadfed8`)
- Use installed Codex model catalog metadata (`451cf3c`)
- Guard missing Provider model metadata (`0b3ba4c`)
- Never fabricate Codex context window in wire catalog (`53fc161`)
- Sync live Codex thread model/effort with Zen route binding (`ed6fa65`)
- Close native effort-default, convergence, and app-server lifecycle gaps (`7feba10`)
- Converge same-model effort-only Terminal changes via native settings evidence (`1c19a2e`)
- Scope monitor settings snapshot to the attached thread (`786ba52`)
- Redesign Skills/Plugins management with daemon-side mutation execution (`0396c23`)
- Collapse Skills chrome to one options control and keep mutations cancelable (`aa89c3c`)
- Add machine-level Codex gateway takeover with config projection (`95b7e73`)
- Proxy Codex Responses-over-WebSocket transparently (`4a63509`)
- Resolve live-control Codex foreground identity deterministically (`9739e7c`)
- Decouple Brain host-switch Grok fixture from wall-clock decay (`4c69c67`)
- Add isolated direct-terminal gateway takeover live proof (`89dc999`)
- Implement Zen-owned Skills package lifecycle for all six Agents (`d690d1f`)
- Retarget gateway on provider default change; pin Settings model metadata (`cb82bc9`)
- Fix isolated terminal proof pane-render race (`f328346`)
- Compile gateway upstream per client so credential injection matches the router (`ca9213a`)
- Merge Skills Manager V2 (daemon-owned skills lifecycle) into main (`b64bc1c`)
- Rebuild Skills Manager package lifecycle (`4457ad2`)
- fix skills manager live contracts (`48468a3`)
- Make Skills Manager local-only (`a6cdcbf`)
- Fix Codex WebSocket response completion lifecycle (`649979c`)
- Fix Interface conversation scroll anchoring (`04ad276`)
- Optimize Interface long conversation rendering (`79fbc4c`)
- Rebuild Skills Manager product surface (`8545624`)
- Harden Skills Manager contracts (`7b1e4c2`)
- Finish Skills Manager V4 device QA (`53fd3d3`)
- Simplify Skills Manager lifecycle (`fe875d2`)
- Harden repository lifecycle and health contracts (`61d6af3`)
- Simplify engineering health audit (`67e40cb`)
- Add direct Plugin management (`cb4ae76`)
- Fix nil Codex metadata fallback (`0df1e9d`)
- Polish Plugins and Skills management (`0b6569f`)

## Install

Download `zen-linux-amd64.tar.gz`, `zen-linux-arm64.tar.gz`, or `zen-darwin-arm64.tar.gz` for your host, plus the Android APK and `SHA256SUMS`. Extract the archive, install `zen` on your `PATH`, run `zen`, then create a pairing link with `zen pair https://your-origin`.

Apple Silicon support requires `tmux` (`brew install tmux`). The macOS binary is cross-built and should be treated as beta until it has completed broader real-device testing.

The iOS Preview is distributed through TestFlight at <https://testflight.apple.com/join/rTKCDzMt>, subject to Apple's beta approval and processing.

## iOS Preview identity

- Bundle: `com.daoleno.zen.preview`
- Marketing version: `0.1.0`
- TestFlight build: `21`
- Source tag: `v0.1.0-beta.20`

The iOS build number is tracked independently from Android because App Store Connect build history can be ahead.

## Android identity

- Package: `com.daoleno.zen`
- `versionCode`: `20`
- ABI: `arm64-v8a`
- Signing certificate SHA-256: `C2:FC:5B:09:B3:86:92:EE:70:59:71:1F:E7:ED:B8:79:4C:E3:65:FE:1C:7A:06:AB:95:4E:5D:D1:BD:CD:A4:FD`

Android may require permission to install from unknown sources or display a Play Protect warning. Obtainium can follow this repository's GitHub Releases. Zen does not currently provide a Play Store package; iOS Preview distribution uses TestFlight.
