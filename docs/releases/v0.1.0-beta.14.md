# Zen v0.1.0-beta.14

This beta contains the reviewed changes on `main` since `v0.1.0-beta.13`.

## What changed

- Fix UTF-8 file preview detection (`bf8a129`)
- Fix terminal Expo Router boundary (`13fecf9`)
- Refine inline code highlighting (`54f12c5`)
- Add live provider and model switching (`75f9142`)
- Normalize OpenCode and Pi Interface conversation projection (`ce46309`)
- Fix terminal font density across Android and iOS (`a831ae2`)
- Set compact terminal grid density default (`31623d9`)
- Fix OpenCode ambiguous admission lifecycle (`f461215`)
- Fix Session list activity time from watcher polls (`42ee214`)
- Add OpenCode Go subscription status to Stats (`91345ae`)
- Show OpenCode Go usage windows and use read-only verification (`3f9b9f1`)
- Confirm OpenCode Go via non-generating invalid-request auth challenge (`a4181de`)
- Read OpenCode Go usage via subscription server-function (`98b6a81`)
- Make dashboard evidence independent and enforce server-function contract (`82a8d83`)
- Accept OpenCode Go 400 invalid_request_error with absent error code (`65ace85`)
- Fix OpenCode Interface tool-call projection (`2c8aa51`)
- Fix OpenCode live turn settlement and stale provider reconcile (`06884ce`)
- Fix OpenCode Go card confirmation against live gateway error shapes (`132b7a2`)
- Replace OpenCode Go subscription Stats with zero-config local usage (`d00d347`)
- Fix mixed-source model cost aggregation for OpenCode usage (`c8e83ce`)
- Add live OpenCode usage rows to Stats screenshot fixture (`4059cfa`)
- Make Stats screenshot fixture synthetic and internally coherent (`dfc76fe`)
- Make stats merge contract fixture synthetic (`fc7211f`)
- Fix slow OpenCode Interface message loading (`e68bfa4`)
- Fix review findings: provider gating, cache race, deletions (`1dec774`)
- Fix equal-count swaps, lock-free SQLite, monotonic versions (`230fc8b`)
- Add canonical per-turn ledger with single reducer for delegated lifecycle (`cc56d62`)
- Add reducer conformance, fault-injection, claim-resolution, E2E wake, and migration tests (`4810f6b`)
- Add held-claim resolution control surface and CLI; anchor admission window on activity start (`1517e7b`)
- Fix claim-retention test to address the original event across note rows (`d48c819`)
- Fix acceptance round-1 P1s: bounded replay, Unknown probing, restart-absent wake, resumable migration, identity-gated abnormal exit (`1d86637`)
- Gate abnormal-exit Failed on the exact recorded process lifetime; decouple liveness from the Provider probe (`2a85bfe`)
- Remove liveness-derived Failed attribution entirely; dead panes resolve Unknown only (`4180308`)
- Fix Calendar OpenCode handoff: 1.18.15 readiness evidence and bounded occurrence retry (`84d49db`)
- Admit bare-tilde home cwd in OpenCode footer readiness evidence (`ebe145e`)
- Redesign Providers settings with one-surface editor and shared input (`a017ad8`)
- Keep Providers editor open on failed saves for retry (`b06ced6`)
- Replace Add Provider picker with direct-connect service rows (`7f506d0`)
- Expand shared Composer on focus and expose inline model switch (`f9c7ec0`)
- Fix review residuals: rebind clearing, 44pt chip, font-scale contract (`a9fa922`)
- Fix Pi Interface live projection: preserve owned --session binding across watcher polls (`5a280a9`)
- Fix P2: strip launcher quoting from owned Pi session path in watcher merge (`5775de4`)
- Fix Composer worklet runtime boundary (`91e5dbf`)
- Fix OpenCode reasoning spinner never settling after terminal turn (`e8db39f`)
- feat: make provider setup client-first (`8831905`)
- fix: simplify provider credentials and HTTP endpoints (`3af35d0`)
- Fix Composer model control for direct Sessions and focus reliability (`cc8e56b`)
- Fix Pi Interface history recovery across daemon restart (`764f01d`)
- Never regress terminal Work from late canonical turn facts (`6ce837b`)
- Replace Composer model explainer with a compact Session model picker (`fe7f243`)
- Auto-bind owned Pi transcripts and never erase populated Interface (`33f4a9f`)
- P2: remove dead sheet state and close the picker when the binding loses live switching (`429340a`)
- Build Plugins and Skills surface v2 with real lifecycle operations (`0c49abe`)
- Round 2: catalog-authoritative plugin lifecycle and calm mobile hierarchy (`335917a`)
- P2: raise Skills controls to the 44pt minimum touch height (`16f8971`)
- Invalidate Pi transcript pin on in-pane startedAt change (`f228202`)
- Gate delegated lifecycle events on canonical per-turn liveness (`7309b6d`)
- Filter Pi transcripts to the current process instance (`d9ffdf9`)
- Surface bounded provider-evidence loss and persist transcript binding (`1352696`)
- Isolate Zen-owned Sessions on a daemon-namespaced tmux server (`ad7771b`)
- Close sub-second restart window with precise provider start (`ca535f3`)
- Collect Pi model usage into Stats (`e468742`)
- Correct universal transport candidate per review findings (`2dbd2f6`)
- Integrate universal agent event transport (`f07b3a2`)
- Add macOS kernel process start and retryable CLK_TCK (`bafe334`)
- Integrate Pi interface history recovery (`6306c68`)
- Advance stale Codex provider activity (`d735b7d`)
- Integrate Pi model usage stats (`56775ae`)
- Own timeline virtualization props in scroll policy (`a6f1656`)
- Collect Zen-owned Pi usage in Stats (`a421f3b`)
- Rewrite Plugins and Skills surface (`d65938a`)
- Render Android chat fade as sibling overlay (`cbbe04c`)
- Fix Plugins and Skills review findings (`bfeea76`)
- Preserve Skills search results on retry (`f847729`)
- Fix canonical turn terminal convergence (`5348216`)
- Fix reusable Session input transaction (`bbb54de`)
- Make Turn Ledger own pending submissions (`b5e81fb`)
- Integrate Plugins and Skills surface V3 (`76714bb`)
- Add durable Brain signal dispositions (`76089a7`)
- Wire Brain signal recovery and control (`37f1fa2`)
- Complete Brain signal continuation ownership (`bd39fc2`)
- Correct Brain signal protocol invariants (`cea47b4`)
- Bind requeued Brain successors and wait producers (`a20a69b`)
- Correct Brain signal authority boundaries (`ed96e53`)
- Make first Brain signals atomic (`9a8ca4c`)
- Keep OpenCode delta fixtures fresh (`bd5098b`)
- Remove redundant Brain signal wake path (`d4abb72`)
- Make Brain owner admission atomic (`0384552`)
- Remove bare-owner Turn admission fallback (`202ccb2`)
- Authorize claimed Brain Host signal turns (`fffb728`)
- Fix Brain Host claim lifecycle (`c1e9656`)
- Require provider admission for Brain continuation (`175da95`)
- Restore Work observatory pull-down layer (`2ccc4fa`)
- Finish Work observatory review corrections (`82975cc`)
- Replace Work dashboard with relationship graph (`babc72a`)
- Correct Work graph accessibility controls (`6d67440`)
- Fix delegated signal terminal authority (`3ab66e9`)
- Correct signal terminal authority comments (`0b45622`)
- Fix delegated spawn admission response (`958cf6d`)
- Make tool activity rows actionable (`897198a`)
- Use caller-visible tmux server for Zen sessions (`998285e`)
- Merge actionable tool-call presentation (`7e22b40`)

## Install

Download `zen-linux-amd64.tar.gz`, `zen-linux-arm64.tar.gz`, or `zen-darwin-arm64.tar.gz` for your host, plus the Android APK and `SHA256SUMS`. Extract the archive, install `zen` on your `PATH`, run `zen`, then create a pairing link with `zen pair https://your-origin`.

Apple Silicon support requires `tmux` (`brew install tmux`). The macOS binary is cross-built and should be treated as beta until it has completed broader real-device testing.

The iOS Preview is distributed through TestFlight at <https://testflight.apple.com/join/rTKCDzMt>, subject to Apple's beta approval and processing.

## iOS Preview identity

- Bundle: `com.daoleno.zen.preview`
- Marketing version: `0.1.0`
- TestFlight build: `15`
- Source tag: `v0.1.0-beta.14`

The iOS build number is tracked independently from Android because App Store Connect build history can be ahead.

## Android identity

- Package: `com.daoleno.zen`
- `versionCode`: `14`
- ABI: `arm64-v8a`
- Signing certificate SHA-256: `C2:FC:5B:09:B3:86:92:EE:70:59:71:1F:E7:ED:B8:79:4C:E3:65:FE:1C:7A:06:AB:95:4E:5D:D1:BD:CD:A4:FD`

Android may require permission to install from unknown sources or display a Play Protect warning. Obtainium can follow this repository's GitHub Releases. Zen does not currently provide a Play Store package; iOS Preview distribution uses TestFlight.
