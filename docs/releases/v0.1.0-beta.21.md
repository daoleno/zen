# Zen v0.1.0-beta.21

This beta contains the reviewed changes on `main` since `v0.1.0-beta.20`.

## What changed

- Guard Codex model catalog contract (`c8f4bc7`)
- Fix first Session file download crash (`9a4cc9a`)
- Harden Expo session file download cleanup (`6dd43b7`)
- Fix session file preview downloads (`6e272c2`)
- Fix Expo session file download options (`1c077c7`)
- Fix session file download names (`aaccaf9`)
- Fix Android SAF session file downloads (`a5abe0a`)
- Wake Brain after durable actionable events (`5891721`)
- Wake Brain after aborted submissions (`f62d2df`)
- Fix bounded Work completion from worker done (`0fb1529`)
- Guard bounded completion from stale session cards (`fb9c3f9`)
- Simplify Brain work event cards (`36279f8`)
- Commit remaining local changes (`b905922`)
- Bind OpenCode transcripts to the agent's own thread (`68c665b`)
- Avoid false delegated ownership loss (`042fb51`)
- Activate autonomous Brain work lifecycle (`abe3f08`)
- Recover idle Brain host delivery (`53b064b`)
- Simplify Work lifecycle review kernel (`00814ec`)
- Fix Brain review admission boundaries (`fdebf56`)
- Stop Host submission from consulting audit turns (`b948894`)
- Add Brain Work activity UI (`6ce8a97`)
- Redesign Skills and plugin management (`dfd67ca`)
- Format model profile gateway responses (`4fd79e5`)
- Move Work activity to Sessions pull surface (`04cbd76`)
- Clean up Work activity lifecycle (`93c8a96`)
- Align Work activity row icons (`7e8430f`)
- Wake Brain from persistent Host activity (`e0b7672`)
- Observe Brain Host provider boundaries (`8570f71`)
- Fix Brain event redelivery identities (`7562a39`)
- Fix Brain work card lifecycle annotations (`5272a8a`)
- Stream large gateway request bodies (`2b0ce1d`)

## Install

Download `zen-linux-amd64.tar.gz`, `zen-linux-arm64.tar.gz`, or `zen-darwin-arm64.tar.gz` for your host, plus the Android APK and `SHA256SUMS`. Extract the archive, install `zen` on your `PATH`, run `zen`, then create a pairing link with `zen pair https://your-origin`.

Apple Silicon support requires `tmux` (`brew install tmux`). The macOS binary is cross-built and should be treated as beta until it has completed broader real-device testing.

The iOS Preview is distributed through TestFlight at <https://testflight.apple.com/join/rTKCDzMt>, subject to Apple's beta approval and processing.

## iOS Preview identity

- Bundle: `com.daoleno.zen.preview`
- Marketing version: `0.1.0`
- TestFlight build: `22`
- Source tag: `v0.1.0-beta.21`

The iOS build number is tracked independently from Android because App Store Connect build history can be ahead.

## Android identity

- Package: `com.daoleno.zen`
- `versionCode`: `21`
- ABI: `arm64-v8a`
- Signing certificate SHA-256: `C2:FC:5B:09:B3:86:92:EE:70:59:71:1F:E7:ED:B8:79:4C:E3:65:FE:1C:7A:06:AB:95:4E:5D:D1:BD:CD:A4:FD`

Android may require permission to install from unknown sources or display a Play Protect warning. Obtainium can follow this repository's GitHub Releases. Zen does not currently provide a Play Store package; iOS Preview distribution uses TestFlight.
